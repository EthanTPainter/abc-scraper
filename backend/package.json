{
  "name": "abc-scraper-backend",
  "version": "1.0.0",
  "description": "Backend for the ABC Scraper",
  "main": "index.ts",
  "scripts": {
    "build": "tsc && cp -r ./node_modules ./dist",
    "clean": "rm -rf ./node_modules"
  },
  "author": "EthanTPainter",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.1203.0",
    "puppeteer": "^16.2.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.102",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.2"
  }
}
